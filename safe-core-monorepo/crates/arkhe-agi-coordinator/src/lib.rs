//! Arkhe AGI Coordinator — Orquestração de agentes autônomos.

pub struct AgiCoordinator;

impl AgiCoordinator {
    pub fn new() -> Self { Self }
}
impl Default for AgiCoordinator {
    fn default() -> Self { Self::new() }
}
